use yew::prelude::*;

#[function_component]
pub(crate) fn Projects() -> Html {
    /*
        * Just print "Still in Development" for now
    */
    html! {
        <section class="view" id="sec:projects">
            <h3>{"Projects Still in Development"}</h3>
        </section>
    }
}